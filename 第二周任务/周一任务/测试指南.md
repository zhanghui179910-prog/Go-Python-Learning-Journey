准备测试数据：首先需要 100 个小文件用来读取。你可以先写一个普通的循环，快速生成 100 个包含简单文本的 .txt 文件放入一个临时目录中。

编写并发读取逻辑：

初始化 var wg sync.WaitGroup。

循环 100 次，每次循环中调用 wg.Add(1)。

使用 go func(fileID int) { ... }(i) 开启 Goroutine去读取对应编号的文件。

在 Goroutine 的闭包函数内部，使用 defer wg.Done() 确保无论读取成功还是失败都能扣减计数器。

主程序等待：在循环结束后调用 wg.Wait()。