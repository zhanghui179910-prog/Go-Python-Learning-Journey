今天的核心任务是把项目“骨架”搭起来，跑通最基本的交互链路 ：
1、搭建项目骨架：初始化你的项目目录和主入口（如 main.go） 。
2、解析命令行指令：实现程序能够接收并识别外部传入的参数（例如输入 add 代表添加任务，输入 list 代表列出任务） 。
3、内存数据流转：能够将内存中的结构体数据，通过终端成功打印出来，验证数据流转正常 。

os 包：学习如何解析命令行参数以及进行基础的文件读写操作 。
：encoding/json 包：掌握数据的序列化（Struct 转为 JSON）与反序列化（解析 JSON 到 Struct） 。
：综合运用：结合本周前几天的知识（特别是结构体、Map、切片和错误处理），处理复杂的文件读写状态与错误反馈