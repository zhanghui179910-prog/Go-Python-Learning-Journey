计划学习主题：抛弃 Python 的 Class 思维：学习结构体 (Struct) 定义、结构体嵌套、值接收者与指针接收者方法的区别 。


计划实操任务：定义一系列包含电子元器件（如电阻、电容）属性的结构体，并为其编写计算总阻值或功耗的方法 。

今日实际达成：

成功手写并理解了 Resistor（电阻）结构体及基于值接收者的 .GetPower() 计算方法。

成功实现了 SeriesCircuit（串联电路）的结构体嵌套，并编写了基于指针接收者的 .TotalResistance() 总阻值遍历相加方法。

超额完成：脱稿盲写了打卡系统的 User 结构体，并完全正确地使用了 *User 指针接收者完成了 .AddCheckin() 打卡天数累加逻辑。